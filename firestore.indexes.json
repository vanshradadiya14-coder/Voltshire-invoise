{
  "//": [
    "Only indexes that a query in this codebase actually needs.",
    "",
    "Commit 6630f47 moved list sorting and filtering on-device (see the",
    "_watchOwned() methods in lib/repositories/), so the single-field ownerId",
    "index Firestore creates automatically now covers every list query. The 16",
    "composite indexes that used to live here were dead config: they cost build",
    "time and storage, and they misled anyone reading the file into thinking the",
    "queries were server-sorted.",
    "",
    "The two below are real. Restore the pruned ones alongside cursor",
    "pagination, which is the change that will put ORDER BY back on the server.",
    "See UPGRADE_PLAN.md, 'Deliberately deferred'."
  ],
  "indexes": [
    {
      "//": "TrashRepository.purgeExpired(): ownerId == uid AND deletedAt < cutoff",
      "collectionGroup": "trash",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "ownerId", "order": "ASCENDING" },
        { "fieldPath": "deletedAt", "order": "ASCENDING" }
      ]
    },
    {
      "//": "CustomerRepository.search(): ownerId == uid ORDER BY nameLower",
      "collectionGroup": "customers",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "ownerId", "order": "ASCENDING" },
        { "fieldPath": "nameLower", "order": "ASCENDING" }
      ]
    },
    {
      "//": "PriceItemRepository.clearSeeded(): ownerId == uid AND isSeeded",
      "collectionGroup": "price_items",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "ownerId", "order": "ASCENDING" },
        { "fieldPath": "isSeeded", "order": "ASCENDING" }
      ]
    },
    {
      "//": "PaymentStageRepository job queries: ownerId == uid AND jobId",
      "collectionGroup": "payment_stages",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "ownerId", "order": "ASCENDING" },
        { "fieldPath": "jobId", "order": "ASCENDING" }
      ]
    }
  ],
  "fieldOverrides": []
}
