# Images

Drop your app images here (e.g. a placeholder company logo `logo.png`).

This folder is referenced by `pubspec.yaml` under `flutter > assets`, so Flutter
requires it to exist. Keep this file so the directory is tracked.
